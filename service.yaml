apiVersion: v1
kind: Service
metadata:
  name: my-first-app-service
spec:
  selector:
    app: my-first-app
    version: blue   # 👈 switch here (blue or green)
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
